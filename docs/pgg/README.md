# PGG — язык процедурной генерации геометрии

Вся документация по PGG собрана здесь.

| Документ | Содержание |
|---|---|
| `geometry_generation_language.md` | **Спецификация языка** — единственный источник правды по семантике: типы и атрибуты (§4), воспроизводимость и зоны (§5), синтаксис (§6), композиция (§7), builtin'ы (§8), отладка (§9), нодовая проекция (§10), диагностики (§11), этапы и критерии (§15), история изменений (§19) |
| `cheatsheet.md` | **Одна страница арт-итераций** — куда смотреть билтины (`docs builtins` / `pgg_docs`), идиомы, грабли семантики, шаблоны MCP; не каталог §8 |
| `implementation.md` | **Заметки по реализации** — этапы E0–E8 по файлам (`src/libs/pgg`), инженерные грабли ANTLR/ядра, CLI PggTool и PggViewer, тестовый корпус `src/tests/pgg/corpus/` + продуктовые примеры `resources/pgg/` и сьюты тестов. Правя код PGG, обновляй его |
| `agent_tooling_plan.md` | **План работ: улучшение инструментов отладки для агента** — демон `PggServe` + MCP `pgg` (картинка узла + статы + диагностики за один вызов по тёплому кэшу), нацеливание камеры, пробники `sample`/`slice`/`check`/`lattice`/`table[where]`/`--eval`, `--fingerprint`/`diff`/голдены-файлы, `docs builtin`/`--explain`, `--profile`; инциденты, приёмка, порядок и оценки |
| `architecture_library_plan.md` | **План: слой раскладки `lib/layout` + архитектурная библиотека `lib/arch`** (v0.3) — три яруса «ядро → домен-нейтральный `layout` → домен → файл здания»; валюта слоя — scope (рамка + `@kind`), библиотека размечает слоты и не материализует терминалы (окна/карнизы живут у потребителя — открытый мир без правки lib), паттерны раскладки как `geo<points>` (fixed/flex/repeat через префиксные суммы), правила как предикаты над scope/edge (`pick_role` + терминал на ребре — код файла здания), скаты крыши — face-scope'ы, стиль точкой-записью, пространственные override'ы; проверенные ограничения ядра (E301 на строки в полях, `select`, `@scale`, enum-литералы только в named-arg), кандидаты в ядро (`roof_wavefront`, `face_scopes`, enum в выражениях), судьба дублей (перенос vs замена), этапы A0–A7; источники — CGA/Houdini/Blender GN |
| `art_session_workflow_plan.md` | **План доработок по итогам арт-сессий `spire_house` (правки готового) и `inn_hotel` (постройка с нуля)** — stateless render и эхо состояния вьювера (липкие `wire`/`ortho`/`target` читаются как свойство модели), cheatsheet + rule для авторинга, вынос архитектурных хелперов в `lib` (многоугольники, подрезка кладки по проёмам, черепица/коньки, скруглённый план и гнутые детали), именованные виды и контакт-лист в viewer, измерения bbox/gap через `pgg_probe`, `select`/`empty_mesh` в ядре, порядок работ «масса → форма → палитра» и правила проверки регрессий |
| `mc_thin_shell_slivers.md` | **Расследование (закрыто, v1.24.2):** marching-cubes плавни-«бахрома» на тонкой оболочке призм — симптом, репрокейс, найденный корень (грань примитива ровно на узлах решётки `mesh_from_sdf`), фикс и тест |
| `serve_rpc.md` | **Контракт `PggServe` + MCP `pgg`** — порт 9878, слоты по каноническому пути `.pgg`, операции (load/params/render/views/probe/export/docs), FBO-кадр, stateless render + `render_state`, именованные виды, автоподнятие демона |
| [`../gallery/`](../gallery/) | **Геройские кадры** арт-примеров для корневого README; как снять и переснять — там же `README.md` |

Код: `src/libs/pgg` (библиотека), `src/libs/pgg_preview` (превью без ImGui), `src/apps/PggTool` (CLI), `src/apps/PggViewer` (нодовая проекция + превью, без TCP), `src/apps/PggServe` (агентский RPC), `src/tests/pgg` (тесты + корпус эталонов `.pgg`), `resources/pgg` (общая `lib/` и оставшиеся арт-примеры), `resources/AmberEstate` (усадьба: коттеджи, spire house, храм, арка, пропы).
