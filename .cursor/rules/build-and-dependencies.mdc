---
description: Windows build, Visual Studio generation, and vcpkg conventions
alwaysApply: true
---

# Build And Dependencies

Canonical guidance lives in `AGENTS.md` → «Сборка (Windows, CMake + vcpkg)», «Сборка (macOS / Linux)».
