"""PGG MCP — agent tooling over the PggServe RPC.

Same Python session on every OS: ``PggSession`` starts PggServe or returns
``need_build``. Entry points: ``python -m tools.pgg_mcp.launch`` (venv) then
``python -m tools.pgg_mcp`` (stdio server).
"""
