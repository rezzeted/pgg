"""PGG MCP — agent tooling over the PggViewer RPC (--serve).

Same Python session on every OS: ``PggSession`` starts PggViewer or returns
``need_build``. Entry points: ``python -m tools.pgg_mcp.launch`` (venv) then
``python -m tools.pgg_mcp`` (stdio server).
"""
